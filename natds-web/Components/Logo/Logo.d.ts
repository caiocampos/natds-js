/// <reference types="react" />
import { ILogoProps } from './Logo.props';
declare const Logo: {
    ({ color, size, model, arialabel }: ILogoProps): JSX.Element;
    displayName: string;
};
export default Logo;
