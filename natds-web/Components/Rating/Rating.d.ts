import * as React from 'react';
import { IRatingProps } from './Rating.props';
export { IRatingProps } from './Rating.props';
/**
 * ## Importing
 *
 * ```
 * import { Rating } from '@naturacosmeticos/natds-web';
 * ```
 */
export declare const Rating: React.ForwardRefExoticComponent<Pick<IRatingProps, keyof IRatingProps> & React.RefAttributes<HTMLDivElement>>;
export default Rating;
